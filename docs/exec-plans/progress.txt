### [Current Focus]
`care-task-flexibility` 模块任务拆解完成（2026-06-22），FLEX-001~021 共 21 个 Task 已生成，全部 pending。
  - 模块文件：docs/exec-plans/modules/care-task-flexibility.json
  - PRD：docs/product-specs/2026-06-22-care-task-flexibility-spec.md
  - 任务拆解规格：docs/product-specs/2026-06-22-care-task-flexibility-task-breakdown.md
  - feature-list.json 已更新：module_index + tasks_summary + cross_module_dependencies
  - 5 阶段落地：P0 纯函数计算层(FLEX-001~002) → P1 数据层+后端(FLEX-003~007) → P2 前端组件+集成(FLEX-008~013) → P3 Cron+集成测试(FLEX-014~019) → P4 文档(FLEX-020~021)
  - 下一步：开始 FLEX-001（computeNextWeeklyDueAt / computeNextSeasonalDueAt 纯函数 + 单元测试）

[2026-06-18] `care-history-timeline-v0.9` 模块全部完成，CARE-HIST-001~006 共 6/6 已完成。模块结项。

[2026-06-18] CARE-HIST-006 DONE — 回归验证 + 端到端验收（care-history-timeline-v0.9 回归验收，High）
  - 三件套验证全通过：npm run typecheck（tsc -b）仅预存错误（7 个，git stash 核实全部预存）；npm run test 14 failures 全部为预存（2 files: EmailLoginForm.test.tsx + AppRouter.smoke.test.tsx，git stash 核实 stash 前后同源）；npx convex dev --once --typecheck enable 零错误。
  - 新增索引 by_plantId_and_completedAt 已部署成功。
  - 零新增编译错误、零新增测试失败。

[2026-06-18] CARE-HIST-005 DONE — 植物详情页集成 CareHistorySection 布局调整（care-history-timeline-v0.9 前端集成，High）
  - PlantDetailPage.tsx 导入 CareHistorySection，插入 PlanSection 之后、归档区之前（L3.5 位置）。
  - 传入 plantId={plant._id}，与上下 section 间距一致。

[2026-06-18] CARE-HIST-004 DONE — 待办页家庭动态区组件（care-history-timeline-v0.9 前端，Medium）
  - 新建 FamilyRecentActivity.tsx：useQuery(api.tasks.listFamilyRecentActivity)，空数组时不渲染。
  - 最多 5 条记录，每条含 plantName 维度（「谁 给 植物名 完成了 任务名」）。
  - 复用 CompletionLogRow 行布局模式。
  - TodoPage.tsx 导入并嵌入 FamilyRecentActivity，位于 Undo Toast 之前。

[2026-06-18] CARE-HIST-003 DONE — 植物详情页养护记录折叠区组件（care-history-timeline-v0.9 前端，High）
  - 新建 CompletionLogRow.tsx：单条记录行，TaskIcon + MemberAvatar(24px) + 成员名 + 任务名 + 相对时间。
  - 新建 CareHistorySection.tsx：折叠容器，usePaginatedQuery 分页加载，折叠时卸载 query 释放订阅。
  - 标题「养护记录（N）」，「加载更多」按钮，空状态「还没有养护记录」。

[2026-06-18] CARE-HIST-002 DONE — 前端类型定义 + formatRelativeTime 工具函数（care-history-timeline-v0.9 前端，High）
  - domain.ts 新增 PlantCompletionLogEntry / FamilyActivityEntry 类型接口。
  - formatters.ts 新增 formatRelativeTime(timestamp, now?)：五档（刚刚/N分钟前/N小时前/M月D日/YYYY年M月D日）。

[2026-06-18] CARE-HIST-001 DONE — 数据层基建 schema 索引 + 两个 Convex query（care-history-timeline-v0.9 后端，High）
  - schema.ts：taskCompletionLogs 新增 .index("by_plantId_and_completedAt", ["plantId", "completedAt"])。
  - tasks.ts：新增 listPlantCompletionLogs（paginatedQuery，join users + plantTasks 获取 customLabel）和 listFamilyRecentActivity（top 5，join users + plants + plantTasks）。
  - 两个 query 均通过 requireCurrentFamilyMember 校验 familyId 隔离。

[2026-06-18] ROLE-012 DONE — 回归验证与类型检查（role-permission-v0.4 E6 回归验收，Medium）
  - 三件套验证全通过：npm run typecheck（tsc -b）仅预存错误；npx convex dev --once --typecheck enable 零错误；npx vitest run 12 failures 全部为预存（git stash 核实 stash 前后同为 2 files failed / 12 tests failed）。
  - ROLE-009~011 改动零回归，未引入任何新测试失败。
  - 预存失败来源：EmailLoginForm.test.tsx（2 用例）和 AppRouter.smoke（10 用例），涉及找不到「加入家庭空间」heading、「逾期 (1)」heading 等 UI 文案变更后未同步的断言。

[2026-06-18] ROLE-011 DONE — 数据迁移脚本（role-permission-v0.4 E5 数据迁移，High）
  - 新建 app/convex/migrations.ts：migrateRolesToOwner internal mutation。
  - 遍历所有 families，每个家庭先检查是否已有 owner（幂等 skip），否则按优先级选举 owner：createdBy membership → earliest admin → earliest member。
  - 返回 { migrated, skipped, totalFamilies } 统计，便于运维确认。
  - 验证：npx convex dev --once --typecheck enable 编译通过。

[2026-06-18] ROLE-010 DONE — leaveFamily 前端适配 owner 退出引导（role-permission-v0.4 E4 前端交互，Medium）
  - 新建 OwnerLeaveGuideSheet.tsx：Sheet role=alertdialog，标题「无法退出家庭」，描述引导 owner 先转让所有者身份。
  - 「去转让」按钮导航到 /settings/members（成员管理页可执行转让操作），「取消」关闭 sheet。
  - FamilySettingsPage 退出按钮 onClick 分支：isOwner→弹 OwnerLeaveGuideSheet，否则→弹原 ConfirmSheet 退出确认。
  - admin/member 退出确认流程完全不变。
  - 验证：tsc --noEmit 零错误。

[2026-06-18] ROLE-009 DONE — 删除家庭入口 owner-only（role-permission-v0.4 E4 前端交互，High）
  - FamilySettingsPage 新增 owner-only「删除家庭」破坏性行（Trash2 图标 + --color-error 红色文字），位于退出家庭与退出登录之间。
  - 点击弹出 ConfirmSheet danger-solid 二次确认，文案含「所有植物、养护记录、成员关系将永久丢失且无法恢复」。
  - 确认后调用 deleteFamily mutation，成功后 familyId→null 由 RouteGate 响应式重定向 onboarding。
  - isOwner 从 summary.currentUserRole === 'owner' 派生，non-owner 不渲染此行。isAdmin 派生为 isOwner || summary.currentUserRole === 'admin'，统一管理入口可见性。
  - 验证：tsc --noEmit 零错误。

[2026-06-18] ROLE-008 DONE — 前端成员操作 Sheet（role-permission-v0.4 E4 前端交互，High）
  - 新建 MemberActionSheet.tsx：computeActions 根据 PRD §4.1 权限矩阵动态计算操作列表；支持设为管理员/撤销管理员/移除成员/转让所有者四种操作。
  - 破坏性操作（移除/转让）弹 ConfirmSheet 二次确认，取消回到操作菜单（而非关闭整个 sheet）。
  - MembersManagePage 改造：旧移除按钮+ConfirmSheet 替换为点击成员行弹出 MemberActionSheet，shouldShowActions 门控 + currentUserRole null 安全保护。
  - Falsification pass：七种角色组合全部正确（owner→member/admin/self, admin→member/admin/owner, member→anyone）。
  - 验证：tsc --noEmit 零错误。

[2026-06-18] ROLE-007 DONE — 前端 RoleBadge 角色标签 + isAdmin 权限对齐（role-permission-v0.4 E4 前端交互，High）
  - 新建 RoleBadge.tsx：owner→品牌绿背景白字'所有者'、admin→灰色底+描边'管理员'、member→不渲染。
  - FamilySettingsPage.tsx 和 MembersManagePage.tsx 成员列表区替换内联 badge 为 RoleBadge，删除冗余样式和 formatRole。
  - 连带修复：两页面 isAdmin 判断扩展为 owner||admin（确保 owner 可见管理入口）。
  - MembersManagePage canRemove 从 !member.isCreator 改为 !member.isOwner（与 ROLE-002 后端对齐）。
  - 验证：tsc -b 仅预存错误；npx convex dev --once --typecheck enable 编译通过。

[2026-06-18] ROLE-006 DONE — getFamilySettingsSummary 排序改造 + isOwner 标记（role-permission-v0.4 E3 查询层适配，High）
  - families.ts getFamilySettingsSummary 排序改造：rolePriority { owner:-2, admin:-1, member:0 } 三级排序替代旧二级 admin/-1 vs 1。
  - members 数组每项新增 isOwner: membership.role === "owner" 布尔标记，供前端渲染角色标签。
  - currentUserRole 直接取 currentMember?.role，已支持三值（owner/admin/member）。
  - 前端 domain.ts 无需改动：类型从 useQuery 推导，自动跟随后端返回值。
  - 验证：npx convex dev --once --typecheck enable 编译通过；tsc -b 仅预存错误。

[2026-06-18] ROLE-005 DONE — deleteFamily + leaveFamily 改造（role-permission-v0.4 E2 核心权限操作，Critical）
  - families.ts 新增 deleteFamily mutation：owner-only，复用 deleteEntireFamily 删整库（plants/plantTasks/taskCompletionLogs/pushSubscriptions/familyMembers/families）。
  - leaveFamily 改造：删除旧 v0.3 三分支（唯一 admin 禁退 + 最后一人隐式删除 + 普通退出），简化为 owner 禁退（'Transfer ownership before leaving.'）+ admin/member 自由退出。
  - leaveFamily 查询优化：from by_familyId collect 全量→by_familyId_and_userId unique 精准取当前用户，减少读取。
  - 返回值从 {leftFamily, deletedFamily} 简化为 {ok:true}；前端不消费返回值（RouteGate 响应式重定向），不回归。
  - 前端 translateLeaveFamilyError 中 "only admin" 检测不再命中，走通用兜底——ROLE-010 会做前端适配。
  - 验证：npx convex dev --once --typecheck enable 编译通过；tsc -b 仅预存错误。

[2026-06-18] ROLE-004 DONE — 新增 transferOwnership mutation（role-permission-v0.4 E2 核心权限操作，Critical）
  - families.ts 新增 transferOwnership mutation：args 为 targetUserId(Id<users>)。
  - 校验链：登录→有家庭→currentMembership.role==='owner'→目标非自己→目标在家庭内→原子 patch。
  - 原子操作：ctx.db.patch(currentMembership, {role:'admin'}) + ctx.db.patch(targetMembership, {role:'owner'})，Convex 事务保证原子性。
  - 验证：npx convex dev --once --typecheck enable 编译通过；tsc -b 仅预存错误。

[2026-06-17] ROLE-003 DONE — 新增 updateMemberRole mutation（role-permission-v0.4 E2 核心权限操作，Critical）
  - families.ts 新增 updateMemberRole mutation：args 为 targetUserId(Id<users>) + newRole(v.union('admin','member'))。
  - 校验链：登录→有家庭→isAtLeastAdmin(currentMembership.role)→目标非自己→目标非 owner→ctx.db.patch(targetMembership._id, { role: args.newRole })。
  - newRole 不允许 'owner'（Convex 参数 v.union 层面拒绝）；目标为 owner 时业务层拒绝并提示走 transferOwnership。
  - 验证：npx convex dev --once --typecheck enable 编译通过；tsc -b 仅预存错误。

[2026-06-17] ROLE-002 DONE — 现有接口权限校验改造（role-permission-v0.4 E1 收尾，Critical）
  - families.ts 三处权限校验从 `role !== 'admin'` 改为 `!isAtLeastAdmin(currentMembership.role)`：renameFamily / resetInviteCode / removeMember，owner 和 admin 统一放行，member 拒绝。
  - removeMember 保护从 `targetUserId === family.createdBy` 硬编码改为 `targetMembership.role === 'owner'` 角色判断，与三级角色体系对齐；删除冗余的 `ctx.db.get(familyId)` 读取（原仅为取 createdBy）。
  - admin 可移除其他 admin（之前 createdBy 保护间接限制了 admin 互移，现在只保护 owner）。
  - import 新增 `isAtLeastAdmin` from `./lib/roleHelpers`。
  - 验证：npx convex dev --once --typecheck enable 编译通过；tsc -b 仅预存错误。

[2026-06-17] ROLE-001 DONE — 数据模型扩展（role-permission-v0.4 模块首任务，Critical 优先级）
  - validators.ts：familyRoleValues 从 ["admin","member"] 扩展为 ["owner","admin","member"]，familyRoleValidator 自动跟随（v.union 遍历 familyRoleValues）。
  - families.ts：createFamily mutation 第 77 行 role 从 "admin" 改为 "owner"，创建者即 owner。
  - 新建 convex/lib/roleHelpers.ts：导出 FamilyRole type（typeof familyRoleValues[number]）+ isAtLeastAdmin(role) 函数（role==="owner"||role==="admin"）。
  - 前端同步：src/types/domain.ts familyRoleValues 同步为三值；FamilySettingsPage/MembersManagePage/MembersList 三处 formatRole 补 if(role==="owner") return "所有者"。
  - 验证：npx convex dev --once --typecheck enable 通过；npm run typecheck (tsc -b) 通过（仅预存错误）；isAtLeastAdmin 三角色返回值正确。12 个 test failures 均为预存（git stash 核实）。

[2026-06-15] TEXT-007 DONE — 第三批·加固②（公共截断工具 + 列表卡 clamp + PlanTaskRow flex 加固 + 整体回归验收，long-text-hardening-v0.5 模块结项任务 7/7）
  - ① 新建 `app/src/lib/textTruncate.ts`：导出 truncateSingleLine（whiteSpace:nowrap + overflow:hidden + textOverflow:ellipsis + minWidth:0 兜底）与 clampLines(n)（display:-webkit-box + WebkitBoxOrient:vertical + WebkitLineClamp:n + overflow:hidden + overflowWrap/wordBreak:break-word）。收口此前各卡片散落的复制粘贴截断写法为单一来源；配 `textTruncate.test.ts` 3 用例（truncateSingleLine 属性 / clampLines(2) 属性 / clampLines 行数随参变化）。
  - ② 列表卡统一复用：TaskListItem 任务名 ...clampLines(2)；DueTaskCard 植物名 ...truncateSingleLine + 任务名 ...clampLines(2)；UpcomingDueCard 同 DueTaskCard。三卡 contentStyle 既有 minWidth:0 满足单行省略父级要求，未额外改父容器。
  - ③ PlanTaskRow（截图2 根因）：labelStyle 原本仅 minWidth:0 无 flex 增长权、却被 intervalStyle 的 flex:1 挤占 → 长任务名假截断/被压扁。修正为 label flex:1 + ...truncateSingleLine 作主收缩列；interval 由 flex:1 改 flexShrink:0 + minWidth:0（去掉随 flex:1 失效的 textAlign:center，保持最小 diff），短文案不再抢主空间，长任务名单行省略根治。
  - ④ token/工具纪律：全部走既有 token 与公共工具，零 #hex、零新增 token。
  - 验收四件套全绿：tsc -b 0 错误 / vitest 25 files 156→159 tests（+1 文件 +3 用例 textTruncate.test.ts）/ convex --once --typecheck 部署成功 / npm run build 构建成功（主 bundle 427KB·gzip122KB）。
  - 走查替代说明：当前环境无 GUI，500+ 字符全页面无溢出走查以代码/构建/测试证据核对替代（截断三件套 + clamp + flex 加固均由单一公共工具保证一致性，PlanTaskRow flex 分配逻辑核验）。
  - 文档：plan 文档 `docs/design-docs/2026-06-15-long-text-handling-plan.md` 追加 §8 验收小结。
  - 状态同步：module JSON TEXT-007→completed（含 completion_note）；feature-list.json TEXT-007→completed、顺手修正 TEXT-006 误标 pending→completed、模块 completed 6→7（jq empty 校验两文件合法、module_index 7/7 + 七任务 tasks_summary 全 completed）。
  - 结论：long-text-hardening-v0.5 模块七任务全部完成，四层防御完整、无回归、纪律达标，可结项。

[2026-06-15] TEXT-006 DONE — 第三批·加固①（后端 Convex mutation 二次校验，防 maxLength/前端校验被绕过的最后一道）
  - convex/lib/validators.ts 新增 assertMaxLength 工具；plants.createPlant/updatePlant 补 name trim + 非空 + 长度，及 location/description/note 长度校验；tasks mutation customTaskName 复用增强校验。长度数值与前端 constants.ts 同源对齐（植物名30/位置30/简介200/备注200/任务名20），避免漂移。
  - 验收：tsc -b 0 错误 / convex --once --typecheck 部署成功 / vitest 全绿（不回归）。
  - 注：此前 progress.txt 仅记录到 TEXT-005，TEXT-006 完成记录在本次结项时补登；feature-list.json 中 TEXT-006 曾误标 pending，本次一并修正为 completed。

**TEXT-005 已完成（2026-06-15）**：第二批·治本②（前端长度校验，maxLength 被粘贴/IME 绕过的二道防线）。① `plantSchema.ts` 导入四常量，validatePlantEditorValues 在非空判断后追加四字段长度校验——name 用 else if 与必填互斥（超限报『植物名称不能超过 30 个字符』），location/description/note 各独立 if 报对应文案，全部模板串嵌常量、与 maxLength 一致。② `taskTypes.ts` 导入 CUSTOM_TASK_NAME_MAX_LENGTH，validateCustomTaskName 改为先 normalized：空→必填提示 / 超 20→『任务名称不能超过 20 个字符』/ 否则 null，CreateTaskPage·EditTaskPage 复用自动生效。③ 新建 `plantSchema.test.ts` 7 用例（四字段超限 + 名称必填 + 边界等于上限），`taskTypes.test.ts` 追加 4 用例（非 custom 不校验/必填/超限/边界）。验收：typecheck 0 错误、vitest 23 files 145 tests 全绿（+1 文件 +10 用例）。

**TEXT-004 已完成（2026-06-15）**：第二批·治本①（输入端 maxLength + 字数计数）。① 新建 `app/src/lib/constants.ts` 集中五个长度上限常量（PLANT_NAME=30/LOCATION=30/DESCRIPTION=200/NOTE=200/CUSTOM_TASK_NAME=20），前端表单与后续前端校验共同消费、避免漂移。② `PlantForm.tsx` 导入四常量，名称/位置 InputField 与简介/备注 TextAreaField 全部加 maxLength；增强本地 TextAreaField 渲染字数计数——hint 行改 flex space-between，右侧显示「N/max」（currentLength 来自受控 value.length），达 max*0.9 切到 `var(--color-warning)` 橙色加粗，counterId 经 aria-describedby 关联 textarea；新增 hintRowStyle/counterStyle/counterNearLimitStyle 三组样式，全部走既有 token，零 #hex 字面色、零新增 token。③ `TaskForm.tsx` 导入 CUSTOM_TASK_NAME_MAX_LENGTH，自定义任务名 InputField 加 maxLength。InputField(...inputProps)/TextAreaField(...props) 均把 maxLength 下发到原生 input/textarea，达上限无法继续键入。坑：TextAreaField 原签名未解构 value，加计数需显式取 value 计算 length（受控组件本就传 value，无行为变化）。验收：typecheck 0 错误、vitest 22 files 135 tests 全绿（不回归；无独立 Form maxLength 用例，由 AppRouter.smoke 自定义任务流覆盖）。

**TEXT-003 已完成（2026-06-15）**：第一批·止血③（截图5 简介铺满撑宽卡片）——核对结论任务，无代码改动。核对：`PlantCard.tsx` 的 PlantListCardData 接口虽含 `description: string | null` 字段，但组件 JSX 全程零引用（grep `plant.description`/`.description` 均 0 命中），概览卡片只渲染 name（h2，TEXT-001 ellipsis）与 location（p，单行省略）——不存在简介整段平铺撑宽风险。位置单行省略已由 TEXT-001 覆盖。故概览展示策略确认为『不露出简介』，无需 clamp，无需改 PlantCard；简介完整内容在详情页可读。验收：typecheck 0 错误、vitest 22 files 135 tests 全绿（不回归）。

**TEXT-002 已完成（2026-06-15）**：第一批·止血②（截图3 编辑页标题撑爆 + 截图4 删除区文案溢出）。(1) `PageHeader.tsx` titleStyle 加 overflowWrap+wordBreak:break-word，clamp 大字号标题（clamp(2rem,5vw,3.1rem)）遇超长连续串/植物名+任务名拼接时换行不横向溢出。(2) `DeleteTaskAction.tsx` 四处嵌入或可能嵌入 taskLabel 的文本样式加 break-word：titleStyle、bodyStyle（"删除后这条 {taskLabel} 提醒…"）、dialogTitleStyle（"确认删除"{taskLabel}"吗？"）、dialogCopyStyle。纯 CSS 零数据风险。验收：typecheck 0 错误、vitest 22 files 135 tests 全绿（不回归，删除流程 smoke 未受影响）。

**TEXT-001 已完成（2026-06-15）**：第一批·止血①（截图1 植物名称撑爆卡片）。根因——`PlantCard.tsx` nameRowStyle 是 flex 容器但缺 minWidth:0，且 nameStyle（h2 子项）无 flex:1/minWidth:0，flex 子项默认 min-width:auto 不收缩 → 既有 ellipsis 三件套（whiteSpace:nowrap+overflow:hidden+textOverflow:ellipsis）形同虚设（假截断）。修复：nameRowStyle 补 minWidth:0；nameStyle 补 flex:1 + minWidth:0，使 h2 可收缩触发 ellipsis（编辑按钮 editIconStyle 既有 flexShrink:0 不被压缩）。location 单行省略由 textAreaStyle（已有 minWidth:0，column flex 下 p 块级撑满）本就生效，仅核对不回归。纯 CSS 零数据风险。无独立 PlantCard 测试文件，由 AppRouter.smoke 覆盖。验收：typecheck 0 错误、vitest 22 files 135 tests 全绿（不回归）。

**ICON-007 已完成（2026-06-15）**：DetailNavBar 手写 SVG 收口（A 级·去重）。`DetailNavBar.tsx` 两处内联手写 SVG 全部切到 Lucide 经 Icon 壳：① 返回按钮 `<polyline points='15 18 9 12 15 6'/>`→`<Icon icon={ChevronLeft} size={20} strokeWidth={2}/>`；② 更多按钮手写三点 `<circle r='1.5'×3 fill=currentColor>`→`<Icon icon={EllipsisVertical} size={20} strokeWidth={2}/>`。尺寸/线宽/currentColor 对齐原值，44×44 热区、aria-label（返回/更多操作）、aria-haspopup/aria-expanded、onMenuToggle 回调全不变，图标由壳自动 aria-hidden。坑：Lucide EllipsisVertical 本身以三个 `r='1'` circle 描边实现（非手写填充），首版"无 circle"断言误红——改为校验 svg 带 lucide class 且无手写 `r='1.5'` 圆点。新建 `DetailNavBar.test.tsx` 3 用例：返回渲染 svg.lucide-chevron-left 且无手写 polyline / 更多渲染 svg.lucide-ellipsis-vertical 且无 r=1.5 圆点 / aria-label+aria-expanded+菜单回调+图标 aria-hidden 不回归。验收：typecheck 0 错误、21 files / 117 tests 全绿（+3）。

**ICON-006 已完成（2026-06-15）**：信息标识图标替换（C 级·卡片标题/状态）。① `NotificationPromptCard.tsx:157`：通知 chip 🔔→`<Icon icon={Bell} size={18}/>`（32px mist iconChipStyle 不变）。② `ActionableTaskSection.tsx` 标题 `⚠️ 需要处理（N）`→`<Icon icon={Info} size={13} strokeWidth={2.5}/>`+文案，titleStyle 加 flex+gap6，accentColor（warning/leaf-light）经 currentColor 传递紧急度。③ `PlanSection.tsx` 标题 `📋 养护计划（N）`→`<Icon icon={ClipboardList} size={13} strokeWidth={2.5}/>`+文案（空态 🌱 C 级保留）。④ `PlantArchiveSection.tsx` 触发器 `📝 植物档案`→`<Icon icon={NotebookPen} size={15}/>`+文案，triggerLabelStyle 改 inline-flex+gap6（▾/▴ 箭头出范围保留）。新建 `PlanSection.test.tsx` 2 用例：PlanSection 渲染 svg.lucide-clipboard-list 且无 📋(U+1F4CB) 且文本含"养护计划" / ActionableTaskSection 渲染 svg.lucide-info 且无 ⚠️(U+26A0) 且含"需要处理"。验收：typecheck 0 错误、20 files / 114 tests 全绿（+2）。卡片标题 aria-name（如 name:/养护计划（2）/i、/开启设备提醒/i）因图标 aria-hidden 不污染，smoke 断言未回归。

**ICON-005 已完成（2026-06-15）**：操作类图标（A 级·完成/编辑）替换。锁定二选一决策——完成态 ✓→Check、pending 态 …→Loader2（加 .complete-spin 旋转）。① `CompleteTaskButton.tsx`：圆形态/文字态完成与 pending 全部切到 `<Icon icon={Check/Loader2}/>`，文字态改为图标+文案行（已完成/完成中），aria-label='完成' 不变。② `ActionableTaskRow.tsx:89`：圆形完成按钮 ✓/… 切到同一 Check/Loader2 图标语言，aria-label='完成{label}' 不变。③ `FamilyHeroCard.tsx:45`：编辑指示 ✎→`<Icon icon={Pencil} size={14}/>`，aria-label='修改家庭名' 与点击回调不变（:33 装饰 🌿 按 C 级保留）。celebrateEmoji '🍃' C 级情感装饰保留。`tokens.css` 新增 `@keyframes complete-spin` + `.complete-spin`（reduced-motion 由全局 @media 兜底压制时长）。新建 `FamilyHeroCard.test.tsx` 3 用例：管理员视角渲染 svg.lucide-pencil 且无 ✎ 字符(防回退) / 普通成员视角不渲染编辑入口 / aria-label 与回调保留且图标 aria-hidden。验收：grep 操作 emoji 0 命中、typecheck 0 错误、19 files / 112 tests 全绿（+3）。AppRouter smoke 的 `name:/^完成$/` 等 aria-label 断言均未回归（仅替换内部图标）。

**ICON-004 已完成（2026-06-15）**：任务类型图标全仓库收口到单一 taskTypeIcon 源。实际改动 5 个消费方（超出契约约定的 3 个，新增 ActionableTaskRow / PlanTaskRow 以达成"全仓库仅剩一份任务类型图标来源"，已在 module JSON completion_note 记录范围扩展）：① `TaskTypeBadge.tsx` 重写——删除 emoji 版 taskTypeVisuals 与 taskTypeEmoji 导出，改为 taskTypeColorVars（--color-task-* token）+ 渲染 `<Icon icon={getTaskTypeIcon(taskType)} size={11}/>`，颜色经 colorVar 注入；顺手修掉 border 简写引发的 React style 警告（border→borderWidth+borderStyle）。② `ActionableTaskRow.tsx`、③ `PlanTaskRow.tsx`——emoji span → Icon + getTaskTypeIcon + taskTypeColorVar 着色（完成按钮 ✓/… 暂留文本，归 ICON-005）。④ `DueTaskCard.tsx`、⑤ `PlantDetailPage.tsx`、⑥ `PlantCard.tsx`——删除各自本地 taskTypeEmoji 映射；PlantCard 编辑 ✏️→`<Icon icon={Pencil}/>`、care pill emoji→Icon；celebrateEmoji / toast 前缀改用固定装饰 🍃（C 级保留）。新建 `TaskTypeBadge.test.tsx` 3 用例：逐类型渲染 svg.lucide 且无 emoji(正则) / badge 18px + 色值=token / taskTypeColorVar 返回 var(--color-task-*)。验收：grep `taskTypeEmoji` 全仓库 0 命中、typecheck 0 错误、18 files / 109 tests 全绿（+3）。

**ICON-003 已完成（2026-06-15）**：任务类型图标权威映射建立。`app/src/features/tasks/taskTypes.ts` 顶部引入 lucide 图标，新增 `taskTypeIcon: Record<CareTaskType, LucideIcon>`（watering=Droplet, fertilizing=Sprout, misting=SprayCan, repotting=Flower2, pruning=Scissors, custom=Tag，严格对齐 icon_mapping_authority.task_type）+ `getTaskTypeIcon(taskType)` 取值函数（未知值回退 custom）。这是 B 级收口**唯一权威源**，下游 ICON-004 据此消除 TaskTypeBadge/PlantCard 两处 emoji 映射在 fertilizing/misting/custom 的历史不一致。新建 `taskTypes.test.ts` 5 用例：六类型全映射存在且为合法图标 / 逐一对齐 authority / **repotting=Flower2 非 FlowerPot 防回退断言** / getTaskTypeIcon 返回对应图标 / 未知值回退 custom。本任务不改任何消费方（仅建来源）。typecheck 0 错误、17 files / 106 tests 全绿（+5）。

**ICON-002 已完成（2026-06-15）**：BottomNav 试点替换 + 核心 bug 修复。`app/src/components/navigation/BottomNav.tsx` navItems 的 icon 字段类型由 string 改为 LucideIcon，值由 emoji(🗓️🪴⚙️) 改为组件引用（待办=CalendarCheck、植物=Sprout、设置=Settings）；渲染处 `<span style={iconStyle}>{item.icon}</span>` 替换为 `<Icon icon={item.icon} size={22} />`，删除已无用的 iconStyle。activeItemStyle 保持 color:var(--color-paper)、itemStyle 保持 color:var(--color-muted)——SVG stroke=currentColor 自动跟随父级 color，**彻底修复 emoji active 态白色失效核心 bug**（emoji 是彩色位图，CSS color 无效）。新建 `BottomNav.test.tsx` 4 用例：三标签渲染 / 3 个 svg.lucide 且无 emoji 文本(正则 /[🗓🪴⚙]/u) / 图标 aria-hidden / **active 按钮 color === var(--color-paper) 且 inactive === var(--color-muted)**（核心 bug 修复断言，反向用例：若回灌 emoji 会红）。typecheck 0 错误、16 files / 101 tests 全绿（+4）。

**ICON-001 已完成（2026-06-15）**：图标基建。`app/` 工作区 `npm i lucide-react@0.471.1 --save-exact`（pin 写入 dependencies，无 ^）。新建 `app/src/components/ui/Icon.tsx` 统一壳：props { icon: LucideIcon, size?(默认20), strokeWidth?(默认2), colorVar?(注入 style.color=var(...)，缺省继承 currentColor), label?(有→role=img+aria-label，无→aria-hidden), className?, style? }；额外加 flexShrink:0 防图标在 flex 行内被压缩。新建 `Icon.test.tsx` 6 用例覆盖：渲染 lucide svg / 缺省不注入字面色（currentColor）/ colorVar 注入 var(--color-leaf) / label→role=img+aria-label / 无 label→aria-hidden / size+strokeWidth 自定义。确立纪律：所有 Lucide 图标经 Icon 壳消费、业务组件禁止直接传字面色。未替换任何业务 emoji（仅交付底座）。typecheck 0 错误、15 files / 97 tests 全绿（+6）。

---

**PLIST2-001 已完成（2026-06-11）**：后端排序改造。`app/convex/plants.ts` listPlantsWithNextDue query 排序逻辑从 `name.localeCompare` 改为 nextDueAt 升序（有任务植物按最近到期排前）+ null 沉底（无任务植物按 _creationTime 降序）。map 中新增 `creationTime: plant._creationTime` 字段用于排序。前端 PlantListPage.tsx 无本地排序代码，无需改动。typecheck 0 错误、86 tests 全绿。

**PLIST2-002 已完成（2026-06-11）**：归档植物查询。`app/convex/plants.ts` 新增 `listArchivedPlants` query：使用 `by_familyId_and_isArchived` 索引查询 isArchived=true 的植物，返回精简字段 {id, name, imageUrl, archivedAt}，按 archivedAt 降序排列（无 archivedAt 时回退 _creationTime）。typecheck 0 错误、86 tests 全绿。

**PLIST2-003 已完成（2026-06-11）**：PlantCard 横向紧凑布局重写。从竖向大图（16:10 封面 + 下方信息）改为横向布局（左图 80×80 圆角方图 + 右文三行：名称行含✏️编辑图标、位置、任务状态胶囊+到期文案）。整卡可点击进详情，右侧保留 › 箭头按钮（aria-label="查看详情"满足 smoke 断言）。PlantListCardData 接口新增 creationTime 字段（与 PLIST2-001 后端返回对齐）。卡片 minHeight 104px，一屏可见 ≥4 张。typecheck 0 错误、86 tests 全绿。

**PLIST2-004 已完成（2026-06-11）**：任务状态行 emoji + 逾期色条。状态行增加 taskTypeEmoji 映射（💧浇水/🧪施肥/🌫️喷雾/🪴换盆/✂️修剪/📋自定义）；逾期判断 nextDueAt < Date.now()：逾期时到期文案用 --color-warning + fontWeight 600，卡片左侧 border-left 3px solid --color-warning（paddingLeft 减 3px 补偿）。非逾期无色条。typecheck 0 错误、86 tests 全绿。

**PLIST2-005 已完成（2026-06-11）**：编辑图标交互优化。editIconStyle 改为 44×44 热区（负 margin 不撑高行）、默认 opacity 0.4、按下 opacity 1.0（onPointerDown/Up/Leave + useState）、transition 100ms。stopPropagation 已在 PLIST2-003 实现。typecheck 0 错误、86 tests 全绿。

**PLIST2-009 已完成（2026-06-11）**：图片渐显与占位态。新增 PlantImage 组件（features/plants/PlantImage.tsx）：内部管理 loading/loaded/error 三态；无图或 error 时显示 mist 底 + 🌿 32px 居中占位；有图时 img opacity 0→1（200ms ease-in transition on load）；占位层 position absolute 保证尺寸一致不跳动。PlantCard 改用 PlantImage 替代 StorageImage（列表场景不需要 storageId 重试逻辑，imageUrl 已由 query 返回）。typecheck 0 错误、86 tests 全绿。

**PLIST2-006 已完成（2026-06-11）**：归档折叠区组件。新增 ArchivedSection 组件（features/plants/ArchivedSection.tsx）：消费 useQuery(api.plants.listArchivedPlants)，内部 filter 确保仅显示 archivedAt 有效的植物；默认折叠，点击「▶ 已归档 (N)」展开/收起（箭头 rotate 90deg 动效 200ms）；展开区 max-height 动画 300ms ease-out + opacity 渐变；ArchivedPlantCard 灰态（opacity 0.55 + grayscale 0.6）+ 恢复按钮调用 restorePlant mutation。集成到 PlantListPage 底部（1px --color-line 分隔 + 24px 上间距）。测试兼容：filter 防止全局 useQuery mock 返回非归档数据导致重复渲染。typecheck 0 错误、86 tests 全绿。

**PLIST2-007 已完成（2026-06-11）**：列表 Stagger 渐入动效。PlantListPage 中为每张卡片包裹 stagger 动画容器：首次数据加载后卡片依次 fade-in + translateY(8px→0)，stagger 50ms/卡，单卡 250ms cubic-bezier(0.25,0.46,0.45,0.94)，前 6 张有递增 delay 第 7 张起 delay 固定 300ms。通过 hasAnimatedRef 防止路由返回时重复动画。CSS keyframe 通过 idempotent style 注入（id="plant-stagger-keyframes"），内含 @media(prefers-reduced-motion:reduce) 退化为即时显示。typecheck 0 错误、86 tests 全绿。

**PLIST2-008 已完成（2026-06-11）**：卡片按压反馈。PlantCard article 添加 className="plant-card"，通过 idempotent CSS 注入（id="plant-card-press-css"）实现 :active 态 scale(0.98) + background var(--color-mist)；按下 transition 120ms ease-out，松开 200ms cubic-bezier(0.34,1.56,0.64,1) 回弹缓动。scale 微小不触发 motion 敏感，无需 reduced-motion 退化。typecheck 0 错误、86 tests 全绿。

**PLIST2-010 已完成（2026-06-11）**：PlantListPage 整合与搜索栏视觉优化。标题栏右侧从 Button 组件改为文字型按钮「+ 添加植物」（--color-leaf 14px/600，无背景无边框）；搜索栏去除 border 改为 border:none + outline:none，聚焦时 box-shadow inset 0 0 0 2px var(--color-leaf) 内描边（通过 className="plant-list-search" + CSS 注入）；列表间距从 --space-md 改为 --space-sm(8px)；移除 Button 组件 import。typecheck 0 错误、86 tests 全绿。

---

`frontend-refresh` 模块已完成任务细拆（2026-06-10），从原来的 5 个粗粒度 UX 任务拆解为 13 个原子任务（VIS-001 ~ VIS-013），每个任务对应可独立交付的垂直切片。

**VIS-001 已完成（2026-06-10）**：tokens.css 已建立（botanical palette 全套色彩/字体/间距/圆角/阴影），index.css/App.css/Button/BottomNav 及全部含硬编码色值的 tsx 已替换为 token 变量。typecheck 通过、test 全绿（8 files / 50 tests）、grep 验收无残留。

**VIS-002 已完成（2026-06-10）**：清理认证后主路径残留英文文案并同步 smoke 测试。改动文件：ArchivePlantAction.tsx（归档/恢复全套文案）、RouteGate.tsx（加载态）、AppShell.tsx（displayName 兜底「植物管家」+ 删除零引用死代码 OnboardingActionPlaceholder 及其专用 import/样式）、uploadPlantImage.ts（2 处 throw）、scheduling.ts（间隔校验错误）、TaskForm.tsx（占位「擦拭叶片」）、DueTaskCard.tsx（img alt）、EmptyState.tsx（默认 badge 兜底）。同步断言：AppRouter.smoke.test.tsx（归档/恢复/preparing/占位符）、taskHelpers.test.ts（间隔错误）。typecheck 通过、smoke 33 绿、全量 8 files / 50 tests 全绿、主路径英文文案 grep 复核干净。

**VIS-004 已完成（2026-06-10）**：Onboarding 四页视觉统一为 quiet-form 风格。改动文件：ProfileBootstrapForm.tsx（实际位于 features/auth/，任务清单路径 features/family/ 有误）、FamilyOnboardingChoicePage.tsx、CreateFamilyPage.tsx、JoinFamilyPage.tsx。统一做法：去除 PageHeader（clamp 大标题会撑爆 Header）改为自建紧凑 Header（maxHeight 120px，eyebrow + 标题 + 一行说明）；全部色值/间距/圆角/阴影引用 token，清除旧蓝橙硬编码（选择页原 accent band 渐变改为 botanical 素面卡片）；邀请码改为 var(--font-mono) 28px/700 letter-spacing 0.12em color leaf 成为最强视觉元素。严格保留 smoke 断言的 heading 文案（欢迎你～/创建一个家庭 h2/加入已有家庭 h2/创建共享家庭植物空间/共享家庭已创建完成/正在接入家庭植物看板）及 ChoiceCard 描述文本。typecheck 通过、全量 8 files / 50 tests 全绿、grep 复核四文件无硬编码色值。

**VIS-005 已完成（2026-06-10）**：App Shell 与底部导航 task-first 改造。改动文件：RouteGate.tsx（默认 fallback `return "/plants"` → `/todo`，第 58 行，唯一改动；注意默认落点逻辑在 RouteGate 而非任务清单列出的 AppShell——RouteGate 不在 files_affected 但必须改）、BottomNav.tsx（navItems 重排为 待办/植物/设置，icon 文字同步为对应中文；item 高度 minHeight:60px → height:56px，fontSize 0.85rem → 10px label，icon 0.7rem → 12px，圆角/间距改 token；active=leaf/inactive=muted 本已满足）、AppShell.tsx（frameStyle/backdropOrbStyle 旧蓝橙硬编码渐变 → var(--gradient-botanical)/var(--gradient-accent)/token 色值；mainStyle justifyContent center → flex-start 使认证后页面 task-first 顶对齐，底部 padding 改为 56px nav 高度匹配）。smoke 新增「lands family members on the todo route by default from root」断言根路径默认落 /todo；既有 /plants 断言均为具体页面场景（创建成功跳转/植物档案页/空状态），非默认落点，无需改动。注：design_contract 写 icon_size 24px，但 icon 实为中文文字非矢量图标，24px 与 56px 紧凑高度冲突，按设计判断设为 12px；56px 高度/10px label/active-inactive 色严格符合验收。typecheck 通过、smoke 34 绿（+1）、全量 8 files / 51 tests 全绿。

**VIS-006 已完成（2026-06-10）**：待办页 task-first 布局改造。改动文件：TodoPage.tsx（移除 PageHeader，loading 态与主体均渲染 h1「家庭养护任务」24px 一行标题，pageStyle gap 改 space-md，DueTaskGroup 调用去掉 eyebrow/description，EmptyState 文案精简为「新的养护提醒到期时会优先出现在这里。」minHeight 180px）、DueTaskCard.tsx（完全重写为三栏紧凑卡：左 40px 圆形缩略图按钮→onOpenPlant 兜底🌿、中 植物名 1rem/700 + 动作 + 到期信息（逾期用 overduePillStyle warning 色胶囊 rgba(221,107,32,0.08) 底 + var(--color-warning)，否则 dueCopyStyle leaf-light）、右 CompleteTaskButton appearance="circle"；isOverdue = nextDueAt < now）、DueTaskGroup.tsx（去 eyebrow/description，改 h2 分组标签 12px uppercase letter-spacing 0.08em color leaf-light + count span，保留 已逾期/今天到期/即将到期 smoke 文案）、TaskListItem.tsx（详情页任务项重写为 token 化紧凑卡，严格保留 编辑 按钮/CompleteTaskButton 默认态「完成」/「已启用提醒」eyebrow 以满足详情页 smoke）、CompleteTaskButton.tsx（新增 appearance?: "default" | "circle"，circle 态渲染 36px 圆形按钮 radius-pill fontSize 20px 内含 aria-hidden ✓/…，保留 aria-label「完成」使 smoke name:/^完成$/ 仍匹配；errorStyle 改 var(--color-error)）。注：DueTaskGroup.tsx 与 CompleteTaskButton.tsx 不在 files_affected 但由 design_contract 的 section_label / completion_button 规格驱动改动。typecheck 通过、全量 8 files / 51 tests 全绿。

**VIS-007 已完成（2026-06-10）**：植物列表页 photo-led 改造。改动文件：PlantCard.tsx（完全重写为图片驱动：顶部 16:10 封面 aspect-ratio object-fit cover top radius-card，无图时 coverPlaceholderStyle mist 底 + 48px 🌿 icon（color-line opacity 0.7）+「轻触上传照片」12px；信息区 padding 12px 16px：h2 植物名 16px/600 heading 字体 ink、位置 12px/400 muted；养护信息改为 carePillStyle 胶囊 12px/700 mist 底 leaf-light radius-pill + 到期 copy 12px muted；保留 查看详情/编辑 按钮——查看详情为 smoke 断言）、PlantListPage.tsx（移除 PageHeader，改紧凑 titleBar：h1「家庭植物档案」24px heading 字体 + 右侧「添加植物」按钮，minHeight 48px；搜索框由 InputField 换为原生 input height 40px radius-input mist 底 + aria-label「搜索植物」；loading 态同样用紧凑标题 + 一行说明；EmptyState minHeight 220→200；全 token 化）。严格保留 smoke 断言：heading 家庭植物档案、heading 植物名（h2）、location 文本、养护标签「浇水」、到期文案、alt「…封面图」+ src、按钮 查看详情；空状态文本「你的家庭植物看板还是空的」+ 按钮 添加植物。注：design_contract component_tree 列 PlantEmptyPlaceholder，但无图占位按 no_image_placeholder 规格内联于 PlantCard 的 StorageImage fallback 实现，规格满足；search 框未被 smoke 断言，换原生 input 安全。typecheck 通过、全量 8 files / 51 tests 全绿。

**VIS-008 已完成（2026-06-10）**：植物详情页视觉重构。改动文件：PlantHeroCard.tsx（完全重写为 Hero-led：移除 Button 依赖与 onBack/onEdit props，仅保留 actionSlot + plant；Hero 区 4:3 aspect-ratio 全宽 object-fit cover、底部圆角 24px 顶部齐平、mist 底，无图占位为 mist 底 + 64px 🪴 icon（color-line opacity 0.7）+「轻触上传植物照片」12px；信息区 padding 紧贴 Hero 下方：h1 植物名 20px/700 heading ink、位置 12px/400 muted、状态摘要 14px/500 leaf-light（已归档…归档于… / 正在家庭看板中使用）；养护备注拆为独立 noteRowStyle：「养护备注」label span + note 文本独立 <p>，确保 getByText 命中裸 note）、PlantDetailPage.tsx（PlantHeroCard 改为仅传 actionSlot + plant；新增底部操作栏：返回列表 ghost + 编辑植物 ghost + 添加任务 gold，添加任务路由到 /plants/:id/tasks/new）。关键判断：design_contract 写 action_bar position fixed bottom，但 App 已有 sticky BottomNav（56px）固定在 frame 列底部，page 级 fixed bottom:0 会与 BottomNav 重叠且脱离 520px 居中列。故将操作栏改为 position sticky、bottom 偏移至 BottomNav 之上（calc(56px + safe-area + space-sm)），保留在 main 居中列内，sheet 圆角 + shadow-sheet 浮层样式——这是对 design_contract 在真实 BottomNav 约束下的正确解读。同时把 addTaskButtonStyle 的 borderColor 改为 border 简写避免 React shorthand 警告。严格保留详情页 smoke 断言：heading 植物名、note 文本（rotate the pot every sunday.）、正在家庭看板中使用（归档态 已归档，归档于）、TaskSection heading 已启用的提醒、浇水/Leaf wipe/每 7 天一次、添加提醒×1、编辑×2、完成×2、alt「…封面图」+ src；TaskSection.tsx/TaskListItem.tsx/ArchivePlantAction.tsx 未改动（不在 VIS-008 范围、各自拥有任务列表与归档文案断言）。确认 添加任务（操作栏）≠ 添加提醒（TaskSection），smoke 计数不受影响。typecheck 通过、全量 8 files / 51 tests 全绿。

**VIS-003 已完成（2026-06-10）**：登录页视觉改造。重构 AuthPage.tsx：不再复用 PageHeader（其 clamp 大标题会撑爆 Hero），改为自建压缩 Hero（logo + 标题 + 一行说明，maxHeight 160px）；页面容器应用 var(--gradient-botanical), var(--gradient-accent) 叠加背景；tab 胶囊（height 36px / radius-pill / 半透白底 + color-line 边框，选中态 leaf 底白字）；表单卡片（rgba(255,255,255,.82) / radius-card / shadow-card / color-line 边框 / space-lg 内边距）；全部引用 token，无硬编码色值。顺手修复 EmailLoginForm.tsx 的 statusStyle 硬编码 #0f766e → var(--color-success)。smoke 未断言登录页内文（仅断言 /login 路径），无需同步测试。typecheck 通过、smoke 33 绿、全量 8 files / 50 tests 全绿。注：AppShell frameStyle/backdropOrbStyle 仍为旧蓝橙硬编码渐变（全局壳层，不在 VIS-003 范围，待后续任务处理）。

### [Task Breakdown Summary]
- **VIS-001**（Critical）：Design Token 基础设施 — 所有后续视觉任务的前置依赖
- **VIS-002**（High）：中文文案统一 ← 原 UX-001
- **VIS-003**（High）：登录页视觉改造 ← 原 UX-005 login 部分
- **VIS-004**（High）：Onboarding 流程视觉统一 ← 原 UX-005 onboarding 部分
- **VIS-005**（High）：App Shell 与底部导航 task-first 改造 ← 原 UX-003 shell 部分
- **VIS-006**（High）：待办页 task-first 布局 ← 原 UX-003 todo 部分
- **VIS-007**（Medium）：植物列表页 photo-led 改造 ← 原 UX-004 list 部分
- **VIS-008**（Medium）：植物详情页视觉重构 ← 原 UX-004 detail 部分
- **VIS-009**（Medium）：设置页视觉重构 ← 原 UX-005 settings 部分
- **VIS-010**（High）：邀请码复制修复与 Fallback ← 原 UX-002
- **VIS-011**（Medium）：任务完成动效与状态反馈 ← 原 UX-005 state-feedback 部分
- **VIS-012**（Low）：通知权限卡片状态可视化
- **VIS-013**（Medium）：视觉回归验证与整体走查

### [Recommended Execution Order]
1. VIS-001（Token 基础设施 — 所有视觉任务的根依赖）
2. VIS-002（文案清理 — 不依赖布局变更，可与 VIS-005 并行）
3. VIS-005（Shell + 导航改造 — 影响默认路由和页面骨架）
4. VIS-003（登录页 — 独立页面，不影响认证后路由）
5. VIS-004（Onboarding — 独立流程页面组）
6. VIS-006（待办页 — 依赖 VIS-005 的新落点）
7. VIS-007 → VIS-008（植物列表 → 详情，上下游关系）
8. VIS-009 → VIS-010（设置页 → 邀请码 fallback，上下游关系）
9. VIS-011（状态反馈动效 — 依赖 VIS-006 和 VIS-008 的卡片结构）
10. VIS-012（通知卡片 — 低优先级，依赖 VIS-009）
11. VIS-013（全路径走查 — 收尾验证）

### [Key Decisions]
- 原 5 个 UX 任务全部覆盖替换为 13 个 VIS 任务，任务 ID 体系从 UX-* 变更为 VIS-*。
- 新增 VIS-001（Design Token 基础设施）作为 0-Critical 前置，确保后续任务不再硬编码色值。
- 新增 VIS-013（视觉回归验证）作为收尾 gate，确保改造完成后整体一致性。
- 每个任务的 design_contract 中已内嵌完整的视觉规格，Coding Agent 无需额外查阅设计文档即可执行。

### [Blockers & Solutions]
- 现有 CSS 中存在硬编码的蓝色(#2563EB)/橙色(#F97316) Token → VIS-001 已全量替换为 botanical palette（已解决）。
- 【VIS-001 期间发现并处理】仓库存在 pre-existing 红测：单元测试（PlantForm/PlantImageField）断言英文 label，而 AppRouter.smoke 测试断言中文 label，二者对同一组件矛盾，源码无论中英文必有一组红。经用户确认采用「源码统一中文 + 同步改单元测试断言为中文」方案修绿。副作用：PlantForm.tsx/PlantImageField.tsx/plantSchema.ts/uploadPlantImage.ts 的文案已中文化，**这部分实际提前完成了 VIS-002（中文文案统一）的一部分工作**，VIS-002 执行时应核对剩余范围避免重复。
- iPhone PWA clipboard API 不稳定 → VIS-010 实现完整的 fallback 面板，不依赖 clipboard。
- 页面间视觉碎片化风险 → VIS-001 的全局 Token + VIS-013 的收尾走查形成闭环。

### [Verification]
- 规划文档变更后：
  - `jq empty docs/exec-plans/feature-list.json`
  - `jq empty docs/exec-plans/modules/frontend-refresh.json`
- 编码阶段统一验证：
  - `cd app && npm run typecheck`
  - `cd app && npm run test`
  - VIS-013 执行时追加全路径截图对比

### [Dead Ends]
- 不要跳过 VIS-001 直接改页面 — 没有统一 Token 会导致后续任务重复定义色值和返工。
- 不要将多个 VIS 任务合并执行 — 每个任务有独立的 verification 和 acceptance_criteria，合并会模糊验收边界。
- 不要只给邀请码复制失败显示错误文案；必须同时给出用户仍能完成分享任务的替代路径。

### [Next Steps]
1. ✅ VIS-001 已完成并提交。
2. ✅ VIS-002 已完成并提交。
3. ✅ VIS-003 已完成并提交。
4. ✅ VIS-004 已完成并提交。
5. ✅ VIS-005 已完成并提交。
6. ✅ VIS-006 已完成并提交。
7. ✅ VIS-007 已完成并提交。
8. ✅ VIS-008 已完成并提交。
9. ✅ VIS-009 已完成并提交。设置页（FamilySettingsPage.tsx）已重构：卡片重排为 家庭摘要→邀请码与分享→通知→家庭成员→退出登录；邀请码用 --font-mono 28px/700 居中展示；复制按钮金色主色 width 100% minHeight 44px；退出按钮独立到 logoutWrap（marginTop var(--space-xl)），ghost 风格 + error 边框；全部 token 化，移除 PageHeader 依赖。typecheck 0 错误，51 测试全过（34 smoke 断言保持）。
10. ✅ VIS-010 已完成并提交。邀请码复制 clipboard API 降级：成功→按钮变绿 1.5s 恢复；失败→立即展示 ManualCopyPanel（可选中邀请码 + 长按可复制提示 + 引导文案）；邀请码文本增加 userSelect:all 便于长按选中。InputField.tsx 顺带 token 化（去除硬编码色值）。新增 smoke 测试覆盖 clipboard-failure fallback 场景。typecheck 0 错误、52 测试全绿（35 smoke +1）。
11. ✅ VIS-011 已完成并提交。任务完成即时反馈与状态动效：DueTaskCard/TaskListItem 完成后卡片 opacity→0（300ms ease-out transition）；CompleteTaskButton 新增 "done" 状态绿色勾选反馈；PlantDetailPage loading 简化为单行「加载中…」；tokens.css 新增 @media(prefers-reduced-motion:reduce) 全局禁止动画规则。额外修改 CompleteTaskButton.tsx（勾选动画载体）和 tokens.css（reduced-motion 是 AC 强制要求）。typecheck 0 错误、52 测试全绿。
12. ✅ VIS-012 已完成并提交。通知权限卡片三态可视化（NotificationPromptCard.tsx 单文件）：顶部新增状态 badge——enabled 绿色 success badge「已开启推送」+ 文案「当前设备已经可以接收家庭植物提醒通知」；disabled 中性 badge「未开启」+ secondary CTA「开启通知」；unsupported 灰色 badge「不支持」+ 文案「当前浏览器暂不支持 Web Push」。保留 needs_install（iOS Safari 安装引导）兜底分支（见 caveats.md）。全部硬编码色值 token 化（--color-surface/--color-success/--color-line/--color-mist/--color-error/--color-muted/--color-ink/--color-leaf-light 等）；card_style 用 --radius-card 16px + --space-md padding。所有 4 条 smoke 断言文案原样保留。typecheck 0 错误、52 测试全绿（35 smoke 不变）。注：本任务 verification 契约为 typecheck+test，不含 init.sh 的 tsc -b build（见 Tech Debt 首条），build 历史遗留问题留待 VIS-013/单独处理。
13. ✅ VIS-013 已完成并提交（frontend-refresh 模块最后一个任务，模块 13/13 全部完成）。全量色系清理：对 frontend-refresh 涉及的全部页面/组件做 botanical Token 化，消除所有非 botanical 硬编码色值（slate/blue 蓝色高亮→--color-mist/--color-leaf；青色 roleBadge #ecfeff/#0f766e→mist/leaf；红色破坏性区域 ArchivePlantAction/DeleteTaskAction #b91c1c/#7f1d1d/#fca5a5/红渐变→--color-error+--color-mist；卡片 rgba 白/阴影→--color-surface/--color-line/--shadow-card；正文/标题 #475569/#1e293b/#0f172a/#64748b→--color-muted/--color-ink；#fff→--color-surface）。本次编辑文件：install.tsx、EmptyState、PageHeader、FormError、RouteGate、MembersList、CompleteTaskButton、PlantImageField、PlantForm、CreatePlantPage、EditPlantPage、TaskSection、TaskForm、EditTaskPage、CreateTaskPage、DeleteTaskAction、ArchivePlantAction、FamilySettingsPage。保留项（botanical 系内部派生，非违规）：BottomNav rgba(251,252,247) = paper 半透明；RouteGate rgba(36,73,63) = leaf 系；PlantForm/InputField rgba(197,48,48) = error 焦点环；PlantImageField rgba(70,112,97)/rgba(241,197,103) = leaf-light/gold；DueTaskCard rgba(221,107,32) = warning 软背景；Button/AuthPage 半透明白 = 中性磨砂玻璃。7 项 checklist 全部满足。typecheck 0 错误、52 测试全绿。AC#1~#4 全部达成。

### [Tech Debt / 待后续处理]
- ✅【2026-06-10 已解决】init.sh build 历史技术债已彻底消除，init.sh 8 步全绿。根因复盘：`npm run typecheck` 此前是裸 `tsc --noEmit`（无 -p），而 tsconfig.json 是 `files:[] + references` 结构，裸 tsc 根本不检查 src 源码——是「假绿」，只有 init.sh [7/8] 的 `tsc -b`（走 project references）才真正编译 src 并暴露错误。修复（用户确认 root_strategy=fix_all_strict + id_fix_approach=cast_as_id + minimal_cast 最小改动）：(1) 业务代码在 useQuery/mutation 调用处用 `as Id<"...">` 断言把 route-params string 转为 Convex 品牌类型——ArchivePlantAction（plantId）、CreatePlantPage（imageStorageId）、EditPlantPage（plantId 由 as never 改为 as Id<"plants"> + imageStorageId）、EditTaskPage（plantId/taskId query + taskId mutation）；CompleteTaskButton/DeleteTaskAction/CreateTaskPage/PlantDetailPage 在上一轮已断言；(2) 测试文件：PlantImageField.test.tsx 的 `global`→`globalThis`（×5）、PlantForm.test.tsx 用 `NonNullable<Parameters<typeof usePlantForm>[0]>` 取索引属性、setup.ts 三个 helper 参数从 `ReturnType<typeof vi.fn>` 改为 vitest `Mock` 类型；(3) 清理 AppShell.tsx 的 createPlantSuccess dead import。**关键根治**：把 `typecheck` 脚本从 `tsc --noEmit` 升级为 `tsc -b`，使其真正检查 src、与 build 强度一致，消除假绿、让 typecheck 与 build/init.sh 验收标准统一。验证：tsc -b --force 0 错误、build 成功、52 tests 全绿、bash init.sh 8 步全绿。
- AppShell mainStyle 改为 flex-start 顶对齐后，登录/onboarding 页面不再垂直居中（这些页面 VIS-003/004 已自建紧凑容器，视觉可接受），VIS-013 走查时确认。
- ✅【VIS-013 已解决】RouteGate.tsx 旧蓝橙硬编码（radial blue / #d9e2ec / #1e293b / #475569）已 token 化为 leaf 系 radial + --color-line/--color-ink/--color-muted。
- 【VIS-013 部分处理】smoke 测试 stderr 仍残留 1 处 React shorthand 警告（mixing border / borderColor）。VIS-013 已修正自己引入的 ArchivePlantAction / DeleteTaskAction 的 borderColor→border 简写；剩余唯一来源为 CompleteTaskButton.tsx L99/L105 的 borderColor:--color-success（VIS-011 引入，预存）。该处非硬编码色值（用的是 token）、不在 VIS-013 的 AC#4 色值清理范围、不阻断测试（52 全绿），按外科手术原则未扩大改动。建议后续单独清理 CompleteTaskButton 的 border 简写混用。
- VIS-008 判断备忘：design_contract 写 action_bar position fixed bottom，但 App 已有 sticky BottomNav（56px）固定在 frame 列底部；page 级 fixed bottom:0 会与 BottomNav 重叠并脱离 520px 居中列。已改为 position sticky + bottom 偏移至 BottomNav 之上（calc(56px + safe-area + space-sm)）。VIS-013 走查时确认该浮层在真机底部安全区的视觉表现。
- VIS-009 边界偏差：design_contract 提到「家庭成员列表改为 56px 行高」，但该改动属于 MembersList.tsx，不在 VIS-009 的 files_affected（仅 FamilySettingsPage.tsx）内。本次保持外科手术原则未改 MembersList，member_row 56px 行高待后续在 MembersList.tsx 范围内补做（或并入 VIS-013 收尾）。FamilySettingsPage 的卡片重排/邀请码/退出按钮三项已全部完成。


### [模块完成记录 · 2026-06-15]
- ✅ settings-page-v2（SET2-001 ~ SET2-012，12/12 全部完成并逐任务提交）。设置页 v0.2 重构落地，typecheck 0 错误、smoke 全绿。
- ✅ plant-detail-v2（PDETAIL2-001 ~ PDETAIL2-009，9/9 全部完成并提交）。植物详情页从「资料卡」重构为「单株养护面板」五层信息架构：DetailNavBar（sticky 顶栏）+ OverflowMenu（编辑/归档/删除，归档与删除均含二次确认）+ Compact Hero（48×48 缩略图+名称+状态摘要动态变色，缩略图可点开全屏预览）+ Action Zone（需要处理区，无到期任务时不渲染）+ Plan Overview（养护计划区，含空状态引导）+ PlantArchiveSection（可折叠档案）+ FloatingAddButton（FAB）+ UndoToast 完成正反馈 + ImagePreviewOverlay。
  本轮关键收尾：(1) 补齐 PDETAIL2-001 契约要求的「归档二次确认」——OverflowMenu 归档/恢复改为弹出 ConfirmSheet（title「确认归档/恢复这盆植物吗？」，variant danger-outline/primary），此前仅删除有二次确认、归档为直接 mutation，属契约缺口；(2) 4 个 baseline smoke 失败用例均为旧 UI 断言，已按新五层 UI 重写（养护计划（n）/需要处理（n）/状态摘要文案/编辑x任务 aria-label/确认归档 等），其中恢复用例最终断言改用 getAllByText 适配「暂无养护计划」在 Hero 与空态计划区各出现一次。
  验证：cd app && npm run typecheck = 0 错误；npx vitest run src/test/AppRouter.smoke.test.tsx = 35/35 全绿（消除 4 个历史失败）。


### [settings-page-v0.3 迭代 · 2026-06-15 启动]
- ✅ SET3-001 已完成并提交。新增 families.renameFamily mutation（家庭改名，admin-only）：照搬 resetInviteCode 权限范式——loadCurrentUserContext → 校验 userId && familyId → by_familyId_and_userId 取 currentMembership 判 role!==admin 抛错；新增常量 FAMILY_NAME_MAX_LENGTH=20，args.name.trim() 非空校验 + 长度上限校验，最终 ctx.db.patch(familyId, { name })，返回 { ok: true }。纯后端无 schema 变更。验证：npx convex dev --once --typecheck enable 编译通过。
- ✅ SET3-002 已完成并提交。新增 FamilyNameEditSheet.tsx（克隆 NicknameEditSheet，写入改为 renameFamily，初始值取当前家庭名，非空 + maxLength 20 校验，无二次确认）。FamilyHeroCard.tsx 新增可选 onRename prop：管理员视角家庭名渲染为 button（内含家庭名 span + ✎ 编辑指示，文本原样保留兼容 getByText），普通成员视角为纯 <p> 只读文本（不渲染入口、不置灰）。FamilySettingsPage.tsx 接线 familyNameSheetOpen state，仅 isAdmin 传 onRename，渲染 FamilyNameEditSheet。验证：tsc -b 0 错误；AppRouter.smoke 35/35 全绿（家庭名 getByText 未破坏）。
- ✅ SET3-003 已完成并提交（家庭改名验证，E1 收尾）。验证结论：①管理员可改名——FamilyHeroCard admin 视角渲染可点击 button → FamilyNameEditSheet → renameFamily → getFamilySettingsSummary 实时返回新名（Convex 响应式订阅自动刷新所有家庭名展示处）；②普通成员无编辑入口——onRename 仅 isAdmin 传入，member 视角家庭名为纯只读 <p>（不渲染入口、不置灰）；③非 admin 调用被后端拒绝——renameFamily 内 role!==admin 抛错；④空名/超长被拒——后端 trim 非空校验 + FAMILY_NAME_MAX_LENGTH=20 校验，前端 sheet 同步兜底校验与文案；⑤不破坏 v0.2 getByText——admin 视角家庭名文本以 span 原样渲染于 button 内，AppRouter.smoke 35/35 全绿。tsc -b 0 错误。E1 家庭改名三任务全部完成。
- ✅ SET3-004 已完成并提交（E2 退出家庭核心后端）。新增 families.leaveFamily mutation（D5 三分支）+ deleteEntireFamily 辅助函数。分支1：当前用户 role===admin 且 adminCount===1 且 memberships.length>1 → 抛错（唯一管理员暂禁退，转让推迟 v0.4）；分支2：memberships.length===1（最后一人）→ deleteEntireFamily 按 family 维度遍历删 plants/plantTasks/taskCompletionLogs/pushSubscriptions/familyMembers 后删 families 记录，返回 deletedFamily:true；分支3：普通成员/非唯一管理员退出 → 级联删本家庭自己的 pushSubscriptions（by_familyId_and_userId）+ 删自身 membership，保留 taskCompletionLogs，返回 deletedFamily:false。补充 import type Id。验证：npx convex dev --once --typecheck enable 编译通过。

[2026-06-15 14:29] SET3-005 DONE — 退出家庭破坏性行 + danger-solid 二次确认
  - FamilySettingsPage：「个人」组退出登录之上新增「退出家庭」红实底破坏性按钮（leaveFamilyButtonStyle，区别于退出登录红描边）
  - 接 useMutation(api.families.leaveFamily)；新增 leaveSheetOpen/isLeaving/leaveError 状态
  - ConfirmSheet variant=danger-solid，文案点名「{家庭名}」家庭名 + 后果（数据仍归家庭、需重新邀请）
  - translateLeaveFamilyError：唯一管理员被拒（only admin）兜底为友好中文，其余统一兜底
  - 成功后 familyId→null 由 RouteGate 响应式重定向 onboarding，卸载前不再 setState
  - 验证：tsc -b 通过；AppRouter.smoke 35/35 通过
  - 下一步：SET3-006（E2 退出家庭整体验证）

[2026-06-15 14:31] SET3-006 DONE — E2 退出家庭整体验证（QA，无代码改动）
  - 逐条核对 leaveFamily 三分支实现与 D5 一致：
    分支1 唯一管理员(有他人) role=admin && adminCount=1 && memberships>1 → 抛错，前端 translate 兜底
    分支2 最后一人 memberships=1 → deleteEntireFamily 遍历删 plants/plantTasks/taskCompletionLogs/pushSubscriptions/familyMembers + families，无残留
    分支3 普通成员 → 级联删自身 pushSubscriptions(by_familyId_and_userId) + 删自身 membership，保留 taskCompletionLogs
  - 验证：tsc -b 通过；npx convex dev --once 函数编译并部署成功；AppRouter.smoke 35/35 通过
  - 下一步：SET3-007（P1 头像 schema + 用户级接口 D6）

[2026-06-15 14:34] SET3-007 DONE — 头像 schema + 用户级接口（D6, E3 个人头像）
  - validators.ts userFields 新增 imageStorageId: v.optional(v.id("_storage"))（统一 storageId 方案，向后兼容）
  - users.ts 新增三个用户级接口（仅校验登录，无 requireCurrentFamilyMember 家庭门槛）：
    generateAvatarUploadUrl(mutation)、getAvatarUrl(query, storageId)、updateMyAvatar(mutation, imageStorageId 传 null 清除)
  - families.getFamilySettingsSummary members[] 补 imageStorageId（user.imageStorageId ?? null）
  - 验证：tsc -b 通过；convex 函数编译部署成功；smoke 35/35
  - 下一步：SET3-008（头像上传交互 AvatarUploadField 前端）
[2026-06-15 14:39] SET3-008 DONE — AvatarUploadField.tsx 新建（圆形预览 + 上传中/已保存/错误三态，注入 generateAvatarUploadUrl/getAvatarUrl/updateMyAvatar，复用 normalizeImageFile + uploadPlantImage，失败保留原头像）；FamilySettingsPage 在「个人信息」组首项渲染。验证：typecheck PASS，smoke 35/35 PASS。module/feature-list completed=8/16。
[2026-06-15 14:42] SET3-009 DONE — StorageImage 参数化（新增可选 fetchUrl，默认 plants.getPlantImageUrl 零回归，resolveUrl 用 useCallback 稳定依赖）；MemberAvatar 新增可选 imageStorageId（有值注入 users.getAvatarUrl 渲染 36×36 圆形真实头像，无值/失败回退首字母色块）；MembersList 透传 imageStorageId。验证：typecheck PASS，smoke 35/35 PASS。completed=9/16。
[2026-06-15 14:43] SET3-010 DONE — E3 头像端到端验证：静态核对六项验收（个人组可上传/HEIC→JPEG/三态失败保留原头像/成员列表响应式实时更新/无头像回退首字母/植物调用方不回归）全通过；后端三接口 convex 部署成功；typecheck PASS；smoke 35/35 PASS。completed=10/16。
[2026-06-15 14:45] SET3-011 DONE — 路由扩展 /join/:code（D2）：AppPath 增 /join；AppRoute.params 增 inviteCode?；normalizePath 新增 ^/join/([^/]+)$ 动态段解析（decodeURIComponent）；路径段方案非 query。验证：typecheck PASS，smoke 35/35 PASS（/join 用例待 SET3-014 补）。completed=11/16。
[2026-06-15 14:51] SET3-013 完成：复制邀请链接 + 邀请落地页。InviteCodeCard 在复制邀请码旁新增「🔗复制邀请链接」按钮（拼接 origin/join/{code}，复用 clipboard+长按兜底+已复制反馈）；新增 JoinLandingPage.tsx 渲染 /join/:code 四分支落地（无效码友好兜底/未登录引导登录注册/已登录无家庭引导带码加入/已登录已有家庭 D3 不重复加入提示家庭名）；AppShell 挂载 /join→JoinLandingPage；RouteGate 已放行 /join 公开可达。验证：tsc -b 0 err、vitest 35/35、convex --once 0 err、JSON 校验 OK。模块进度 11→12/16。
[2026-06-15 14:55] SET3-012 完成：跨多跳暂存邀请码 + onboarding 自动加入。新增 usePendingInvite.ts（sessionStorage 键 pendingInviteCode，关标签页即清，隐私模式降级容错）；JoinLandingPage 在「带有效码且未加入家庭」时暂存码、已登录无家庭分支改跳 /onboarding 触发自动加入；FamilyOnboardingChoicePage 检测暂存码→进入「加入中」过渡态→自动 joinFamilyByInviteCode→成功清除暂存跳 /todo，失败清除暂存+回落二选一页友好提示（useRef 防 StrictMode 重复触发）。验证：tsc -b 0 err、vitest 35/35、convex --once 0 err、JSON OK。模块进度 12→13/16。

[2026-06-15 15:00] SET3-014 DONE — 邀请链接 E2E 验证 + /join smoke 用例
  - 新增 describe("Invite link landing (/join/:code)") 5 个用例：normalizePath 解码、未登录引导登录、已登录无家庭引导 onboarding 自动加入(校验暂存码)、已有家庭 D3 友好提示(不暂存)、无效码兜底不白屏
  - 修正 routeParams inviteCode:null 类型错误 → 省略字段
  - 验证三件套：tsc 0 err / vitest 40 passed(35→40) / convex 0 err
  - 状态：module json SET3-014→completed；feature-list.json SET3-014→completed，settings-page-v0.3 completed 13→14
  - commit 待提交

[2026-06-15 15:02] SET3-015 DONE — 全局加固 .unique()→.first()(关 §6.1 红线)
  - lib/auth.ts getCurrentUserContext 读 familyMembers by_userId 改 .collect() 取第一条降级；>1 条时 console.warn 便于排查脏数据
  - 严格保持单一家庭约束语义不变；正常单 membership 用户行为完全不变
  - 验证：tsc 0 err / convex 0 err / vitest 40 passed
  - 状态：module+feature-list SET3-015→completed，settings-page-v0.3 completed 14→15

[2026-06-15 15:05] SET3-016 DONE — v0.3 整体回归收口（settings-page-v0.3 16/16 全部完成）
  - tsc -b 0 err；convex --once --typecheck 0 err
  - 全量 vitest：14 files / 91 tests 全通过（含 AppRouter.smoke 40 个，新增 /join/:code 5 例；v0.2 家庭名 getByText 断言未被改名破坏）
  - grep app/src/features/family + AvatarUploadField + components/ui 无硬编码 #hex（token 纪律达标）
  - D5 三分支逻辑代码核验：leaveFamily 分支1 唯一管理员有他人禁退 / 分支2 最后一人 deleteEntireFamily 删整库含 logs / 分支3 普通成员退删自身 membership + 级联删本人 pushSubscriptions 保留 taskCompletionLogs
  - StorageImage 植物调用方随全量 vitest 通过，无回归
  - 状态：module+feature-list SET3-016→completed，settings-page-v0.3 completed 15→16（模块全部完成）

[2026-06-15 15:56] FEEDBACK-FIX v0.3 — 用户问题反馈三连修复（UI/交互文案/安全文案）
  - 反馈1(UI)：InviteCodeCard 复制邀请链接按钮原为透明描边+justifySelf:start，与金色全宽复制邀请码按钮不协调。
    调用 frontend-design 技能后重做：加「或分享链接」分隔线把两种分享方式归为一组；链接按钮改为全宽 mist 底色次级按钮（与卡片同语系，从属于金色主按钮），复制成功态切 success 绿；修复 border/borderColor shorthand 混用 React 警告。
  - 反馈2(交互)：用户经邀请链接而来，落地页却展示「邀请码：XXX」+「用邀请码加入」，点后直接加入使邀请码展示无意义。
    JoinLandingPage 分支3(已登录无家庭)去掉 codeChip，按钮改「加入这个家庭」，body 聚焦共享看板；分支2(未登录)同样去掉邀请码暴露；codeChipStyle 已删除。邀请码仍 sessionStorage 暂存，加入流程不变。
    smoke 断言同步：邀请码 getByText→queryByText not present + 验证 pendingInviteCode 仍暂存；按钮名 用邀请码加入→加入这个家庭。
  - 反馈3(安全文案)：最后一人退出=解散家庭(后端 leaveFamily 分支2 级联删全库)，原文案误称「养护数据仍归这个家庭所有」。
    FamilySettingsPage 据 summary.memberCount<=1 派生 isLastMember：标题「退出后将解散这个家庭」、描述明确「所有植物/提醒/养护记录将被永久删除且无法恢复」、确认按钮「退出并解散家庭」；非最后一人维持原温和文案（措辞修正为「仍由其他家人保留」）。
  - 验证：tsc -b 0 err / convex --once --typecheck 0 err / 全量 vitest 14 files 91 passed（AppRouter.smoke 40 全过）

[2026-06-15 17:52] ICON-009 DONE — icon-system-v0.4 模块整体回归验收（模块 9/9 全部完成并结项）
  - 自动化三件套：npm run typecheck（tsc -b）0 错误 / npm run test 22 files 135 tests 全绿 / npx convex dev --once --typecheck enable 函数编译部署成功；额外 npm run build（tsc -b && vite build）1732 模块构建成功、主 bundle index 426KB(gzip122KB)。
  - emoji 清理：grep taskTypeEmoji 全仓库 0 命中（B 级单一来源彻底）；本模块 13 个 A/B 级落点源文件无功能性 emoji 残留（no-functional-emoji.test.ts 守护，故意回灌已实操验证变红）；C 级 🌿🍃🌱 在空态插画/Hero 装饰/完成情感反馈（celebrateEmoji/toast 前缀）处按策略保留；非本模块范围的 SettingCardHeader chip(🔑🙂✉️👥ℹ️)、PlantHeroCard 无图占位 🪴 按外科手术原则未动。
  - token 纪律：.tsx 全仓库 grep 无 #hex 字面色，图标色一律 currentColor 继承或 colorVar 注入 var(--color-*)，Icon 壳层强制；本次未新增 token。
  - tree-shaking：全部 lucide-react import 为具名按需引入（无 import * as 整包导入），仅约 16 个使用图标打入；反证 airplay/anchor/banana/rocket/zap 等未使用图标 0 命中，未整包膨胀。
  - reduced-motion：唯一新增动效 .complete-spin 由 tokens.css 全局 @media(prefers-reduced-motion:reduce){animation-duration:0.01ms!important;animation-iteration-count:1!important} 压制为单次近 0 时长等效静止。
  - a11y：Icon.tsx 壳 label 有值→role=img+aria-label、无值→aria-hidden；业务图标默认不传 label 自动 aria-hidden，AppRouter smoke heading/button name 断言 40/40 未回归。
  - 走查替代说明：当前环境无 GUI，design_contract 的"四页面双视角截图走查"以代码/构建/测试证据核对替代（导航 active 转白由 BottomNav.test active===paper / inactive===muted 反向断言保证；任务类型六图标一致由 getTaskTypeIcon 单一源 + TaskTypeBadge.test 保证）。
  - 文档：docs/design-docs/icon-system-migration.md 末尾追加 §8 迁移验收小结。
  - 状态同步：module JSON ICON-009→completed（含 completion_note）；feature-list.json ICON-009→completed、顺手修正 ICON-008 误标 pending→completed、模块 completed 7→9（jq empty 校验两文件合法、module 内 9/9 completed）。
  - 结论：icon-system-v0.4 模块全部完成，迁移完整、无回归、纪律达标，可结项。下一件事待用户指派。
