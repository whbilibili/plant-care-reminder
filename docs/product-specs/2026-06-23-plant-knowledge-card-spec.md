# 植物知识卡片

**版本**：v1.0
**日期**：2026-06-23
**关联文档**：承接 [MVP 需求文档](./2026-06-05-mvp-requirements.md)（§7.2 植物管理）、[迭代储备大纲](./2026-06-17-iteration-backlog-outline.md)（P2-1）、[养护任务灵活性](./2026-06-22-care-task-flexibility-spec.md)（排期建议联动）
**文档定位**：为用户提供植物品种级别的养护知识引导，在创建植物时自动推荐养护参数，在详情页展示品种养护小贴士，降低新手用户的养护决策门槛。

---

## 0. 本次迭代要解决的核心问题

MVP 中用户创建植物时，需要自行决定浇水频率、施肥间隔等参数，但大多数用户（尤其是新手）并不清楚具体数值该填多少。这导致两个体验缺口：

- **"刚买了一盆龟背竹，不知道几天浇一次水，随便填了个 3 天，结果浇多了烂根"** —— 缺乏养护知识引导：用户在设置任务间隔时没有参考依据，只能凭感觉填写，容易导致养护不当。
- **"我家有 10 盆不同的植物，每次都要去百度查养护方法，再回来设置"** —— 信息获取路径长：用户需要在应用外查找养护知识，再手动转化为应用内的任务配置，流程割裂。

本次通过引入**植物品种知识库**，在创建植物和查看详情时提供品种级别的养护建议，让用户"不用查就知道怎么养"。

---

## 1. 产品目标

让用户在创建植物时能够快速获得该品种的推荐养护参数（浇水频率、光照需求、施肥建议等），一键应用到任务配置中。同时在植物详情页提供常驻的养护知识卡片，作为日常养护的参考指南。

---

## 2. 用户场景

### 场景 1：新手创建植物时获得推荐

用户刚入坑养植物，买了一盆绿萝。在创建植物页面输入"绿萝"后，系统自动匹配到品种库中的绿萝条目，弹出推荐卡片："绿萝 — 浇水建议每 3~5 天一次，喜散射光，忌强光直射"。用户点击"应用推荐"，浇水任务自动填入间隔 4 天，省去了查资料的步骤。

### 场景 2：详情页查看养护贴士

用户养了一盆琴叶榕半年了，最近叶子发黄。她打开植物详情页，看到知识卡片区域写着"琴叶榕 — 冬季减少浇水，保持盆土微干；叶片发黄可能是浇水过多或光照不足"。她意识到可能是冬天没有减少浇水频率，于是去编辑任务间隔。

### 场景 3：品种未匹配时的降级体验

用户养了一盆比较小众的"鹿角蕨"，品种库中没有收录。创建时系统不展示推荐卡片，用户正常手动填写参数。详情页的知识卡片区域显示"暂无该品种的养护指南，欢迎自行记录养护心得到备注中"。

### 场景 4：推荐参数与个人经验不同

用户是资深植物爱好者，知道自己家的环境比较干燥，绿萝需要每 2 天浇一次。系统推荐 3~5 天，用户选择忽略推荐，手动填写 2 天。推荐是建议而非强制。

---

## 3. 功能设计

### 3.1 品种知识库

**数据结构**（前端静态 JSON）：

```typescript
interface PlantSpecies {
  id: string;                    // 唯一标识，如 "monstera-deliciosa"
  names: string[];               // 品种名称（含别名），如 ["龟背竹", "蓬莱蕉"]
  category: string;              // 分类，如 "观叶植物"
  difficulty: "easy" | "medium" | "hard";  // 养护难度
  care: {
    watering: {
      intervalRange: [number, number];  // 建议浇水间隔范围（天），如 [3, 5]
      tip: string;                       // 浇水贴士
    };
    light: {
      level: "low" | "medium" | "bright" | "direct";  // 光照需求
      tip: string;
    };
    fertilizing: {
      intervalRange: [number, number];  // 建议施肥间隔范围（天），如 [14, 30]
      tip: string;
      seasonalNote?: string;            // 季节性说明
    };
    humidity?: {
      level: "low" | "medium" | "high";
      tip: string;
    };
    temperature?: {
      range: [number, number];          // 适宜温度范围（℃）
      tip: string;
    };
  };
  commonIssues: string[];        // 常见问题提示，如 ["叶尖发黄可能是空气太干"]
  seasonalNotes: {
    springSummer: string;        // 春夏养护要点
    autumnWinter: string;        // 秋冬养护要点
  };
}
```

**初期规模**：预置 80 种常见室内绿植数据，覆盖中国家庭最常养的品种。

**数据来源**：人工整理 + 园艺资料校验，以 JSON 文件形式存放在前端代码中（`app/src/data/plant-species.json`）。

### 3.2 品种匹配逻辑

**匹配时机**：用户在创建植物页面的"植物名称"输入框中输入文字时触发。

**匹配规则**：

- 对用户输入的植物名称，在品种库的 `names` 数组中进行模糊匹配（包含匹配）。
- 匹配优先级：完全匹配 > 前缀匹配 > 包含匹配。
- 匹配结果最多展示 3 个候选品种（如果有多个匹配）。
- 匹配不区分大小写，支持中文和拉丁学名。

**匹配结果展示**：

- 输入框下方出现匹配候选列表（类似搜索建议）。
- 用户点击某个候选品种后，触发推荐卡片展示。
- 用户也可以忽略候选列表，继续手动填写。

### 3.3 创建植物时的推荐卡片

**触发条件**：用户在创建植物页面选择了一个匹配的品种。

**卡片内容**：

- 品种名称 + 养护难度标签（"容易养护" / "中等难度" / "需要经验"）。
- 浇水建议：推荐间隔范围 + 一句话贴士。
- 光照需求：图标 + 一句话描述。
- 施肥建议：推荐间隔范围 + 季节性说明。

**操作按钮**：

- "应用推荐"：将推荐的浇水间隔（取范围中间值）自动填入任务创建表单。
- "仅供参考"：关闭卡片，用户自行填写。

**"应用推荐"行为**：

- 自动创建浇水任务，间隔取推荐范围的中间值（向上取整）。如推荐 [3, 5]，则填入 4 天。
- 如果品种有施肥建议，同时预填施肥任务的间隔。
- 用户仍可在填入后手动修改数值。

### 3.4 详情页知识卡片

**位置**：植物详情页，位于任务列表区域下方，作为独立的信息区块。

**展示条件**：

- 植物的 `speciesId` 字段有值（即创建时匹配了品种）。
- 如果未匹配品种，展示兜底文案。

**卡片内容**：

- 标题："养护指南 · {品种名}"。
- 分区展示：浇水 / 光照 / 施肥 / 常见问题。
- 每个分区一个图标 + 一段简短文字（1~2 行）。
- 底部展示当前季节的养护要点（根据月份自动切换春夏/秋冬文案）。

**交互**：

- 卡片默认折叠，只展示标题行 + "查看养护指南"按钮。
- 点击展开完整内容。
- 展开/折叠状态不持久化（每次进入详情页默认折叠）。

### 3.5 品种关联存储

**plants 表新增字段**：

```
speciesId: v.optional(v.string())  // 关联的品种库 ID，如 "monstera-deliciosa"
```

**写入时机**：用户在创建植物时选择了匹配的品种，将品种 ID 存入。

**不强制关联**：用户可以不选择任何品种（手动填写植物名），此时 `speciesId` 为空。

---

## 4. 数据模型变更

### 4.1 plants 表新增字段

```
speciesId: v.optional(v.string())  // 品种库 ID，用于关联前端静态知识库
```

**向后兼容**：`v.optional` 保证老数据无需迁移。老植物的 `speciesId` 为 undefined，详情页展示兜底文案。

### 4.2 无新增表

品种知识库为前端静态 JSON，不存入 Convex。理由：

- 数据量小（80 条，约 50KB），前端直接打包无性能问题。
- 数据更新频率极低（季度级别），不需要动态更新能力。
- 避免增加后端复杂度和查询开销。

### 4.3 索引评估

无需新增索引。`speciesId` 仅用于前端展示时的本地查找，不参与后端查询条件。

---

## 5. 接口变更

### 5.1 createPlant 改造

新增可选参数：

```
speciesId: v.optional(v.string())
```

创建植物时，如果用户选择了品种，将 `speciesId` 一并写入。

### 5.2 updatePlant 改造

支持修改 `speciesId`（用户可以后续关联或取消关联品种）。

### 5.3 无新增后端接口

品种匹配和知识展示均为纯前端逻辑，不需要新的 Convex function。

---

## 6. 前端交互设计

### 6.1 创建植物页 — 品种匹配

- 用户在"植物名称"输入框输入 2 个字符后，开始触发品种匹配。
- 匹配结果以下拉列表形式展示在输入框下方（最多 3 条）。
- 每条候选项展示：品种名 + 难度标签 + 分类。
- 点击候选项：将品种名填入输入框 + 展示推荐卡片 + 记录 speciesId。
- 无匹配结果时不展示下拉列表。
- 用户可以忽略下拉列表继续输入（列表在失焦后消失）。

### 6.2 创建植物页 — 推荐卡片

- 选择品种后，在表单区域上方展示推荐卡片（浅绿色背景 `--color-leaf-light`）。
- 卡片内容紧凑：品种名 + 难度 + 浇水/光照/施肥各一行。
- 底部两个按钮："应用推荐"（主按钮）+ "仅供参考"（文字按钮）。
- 点击"应用推荐"后卡片收起，表单中对应字段自动填入推荐值，toast 提示"已应用推荐参数"。
- 点击"仅供参考"后卡片收起，不做任何填充。

### 6.3 详情页 — 知识卡片区块

- 位于任务列表下方，与任务区域间距 16px。
- 折叠态：单行展示"🌱 养护指南 · {品种名}"+ 右侧 chevron-down 图标。
- 展开态：卡片展开，内部分 4 个小区块（浇水/光照/施肥/当季要点），每块一个图标 + 文字。
- 展开/折叠动画：height + opacity 过渡，200ms ease-out。
- 未关联品种时：展示"暂无该品种的养护指南"灰色文案，不可展开。

### 6.4 编辑植物页 — 品种关联

- 编辑页面的植物名称输入框同样支持品种匹配。
- 如果植物已关联品种，输入框下方展示当前关联的品种标签（可点击"×"取消关联）。
- 修改植物名称时重新触发匹配，用户可切换关联的品种。

---

## 7. 品种知识库管理

### 7.1 初期数据范围

首批 80 种植物覆盖以下分类：

- 观叶植物（30 种）：绿萝、龟背竹、琴叶榕、散尾葵、橡皮树、虎皮兰、吊兰、常春藤、鹤望兰、龙血树等。
- 多肉植物（20 种）：芦荟、仙人掌、玉露、石莲花、熊童子、多肉拼盘等。
- 开花植物（15 种）：栀子花、茉莉、月季、蝴蝶兰、长寿花、三角梅等。
- 草本/蕨类（10 种）：铁线蕨、波士顿蕨、薄荷、罗勒等。
- 水培植物（5 种）：铜钱草、水培绿萝、富贵竹等。

### 7.2 数据维护策略

- 初期：开发团队人工整理，参考权威园艺资料。
- 中期：根据用户创建植物时的高频名称（未匹配到品种库的），定期补充新品种。
- 长期（本次不做）：接入外部植物 API 或允许社区贡献。

### 7.3 数据文件位置

```
app/src/data/plant-species.json       // 品种知识库主文件
app/src/data/plant-species.types.ts   // TypeScript 类型定义
app/src/lib/speciesMatch.ts           // 品种匹配工具函数
```

---

## 8. 边界与异常处理

| 场景 | 处理 |
|------|------|
| 用户输入的植物名不在品种库中 | 不展示推荐卡片，正常手动创建；详情页展示兜底文案 |
| 品种库 JSON 加载失败 | 静默降级，不展示匹配和推荐功能，不影响核心创建流程 |
| 用户输入拼音或错别字 | 当前版本不支持拼音/模糊纠错匹配，仅支持中文名精确/包含匹配 |
| 品种有多个别名 | names 数组包含所有别名，任一匹配即可 |
| 推荐间隔范围为 [3, 3]（单值） | 直接填入 3，不做中间值计算 |
| 用户应用推荐后又手动修改 | 以用户最终提交的值为准，推荐值不具有约束力 |
| 老植物无 speciesId | 详情页展示兜底文案；用户可通过编辑页面后续关联品种 |
| 品种库更新后 speciesId 对应条目被删除 | 前端查找不到时展示兜底文案，不报错 |

---

## 9. 验收标准

### 9.1 品种匹配

- [ ] 创建植物页输入 2 个字符后触发品种匹配
- [ ] 匹配结果最多展示 3 条候选
- [ ] 完全匹配优先于包含匹配
- [ ] 点击候选项后正确展示推荐卡片
- [ ] 无匹配时不展示下拉列表，不影响正常创建

### 9.2 推荐应用

- [ ] 点击"应用推荐"后浇水间隔自动填入推荐中间值
- [ ] 施肥建议同时预填（如品种有施肥数据）
- [ ] 用户可在应用推荐后手动修改数值
- [ ] 点击"仅供参考"后卡片收起，不填充任何值
- [ ] speciesId 正确写入 plants 文档

### 9.3 详情页知识卡片

- [ ] 已关联品种的植物展示可折叠的知识卡片
- [ ] 卡片内容包含浇水/光照/施肥/当季要点四个区块
- [ ] 当季要点根据当前月份自动切换春夏/秋冬文案
- [ ] 未关联品种的植物展示兜底文案
- [ ] 折叠/展开动画流畅

### 9.4 向后兼容

- [ ] 老植物（无 speciesId）正常展示，不报错
- [ ] 品种库加载失败时核心功能不受影响
- [ ] 创建植物流程在无品种匹配时与 MVP 行为一致

### 9.5 数据质量

- [ ] 品种库包含至少 80 种常见室内植物
- [ ] 每种植物的浇水/施肥间隔建议合理（经园艺资料校验）
- [ ] 品种名称覆盖常见别名

---

## 10. 后续候选（本次不做）

- 拼音输入匹配（输入 "lvluo" 匹配"绿萝"）
- 拍照识别植物品种（接入图像识别 API）
- 用户贡献养护经验（UGC 知识库）
- 品种库动态更新（后端存储 + 管理后台）
- 基于用户所在城市的本地化养护建议
- 品种间的养护对比功能
- 养护难度评分和新手推荐植物列表

---

## 11. 设计决策记录

| # | 决策 | 理由 |
|---|------|------|
| D1 | 品种库用前端静态 JSON 而非后端存储 | 数据量小（80 条 ≈ 50KB）、更新频率极低（季度级）、避免增加后端复杂度和查询开销；后续如需动态更新再迁移到后端 |
| D2 | 匹配触发阈值为 2 个字符 | 1 个字符匹配结果太多（如"绿"匹配到绿萝、绿宝石等），2 个字符能有效收窄范围 |
| D3 | 推荐是建议而非强制 | 尊重用户的个人经验和环境差异；强制应用会让资深用户反感 |
| D4 | 详情页卡片默认折叠 | 避免信息过载，知识卡片是辅助信息而非核心操作区；用户需要时主动展开 |
| D5 | speciesId 存入 plants 表而非仅前端本地 | 支持跨设备一致体验；为后续"按品种筛选"等功能预留数据基础 |
| D6 | 初期 80 种而非更多 | 平衡数据质量和覆盖率；80 种覆盖 90%+ 的家庭常见绿植；后续根据未匹配高频词补充 |
